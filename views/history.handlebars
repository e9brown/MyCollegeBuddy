<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Previous Entries</title>
<!-- HELP BUTTON HEADER CODE BLOCK -->
    <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
    <script src="//code.jquery.com/jquery-1.10.2.js"></script>
    <script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
    <script>
      //$(function() {
	    //$( "#dialog" ).dialog();
      //});
	  
	  $(document).ready(function () {
        //$('#dialog').dialog(); 
        $('#dialog_link').click(function () {
          $('#dialog').dialog();
          return false;
        });
      });
    </script>
	<!-- END HELP BUTTON HEADER CODE BLOCK -->
  </head>

  <body class="bg">
    <div class="jumbotron">
      <h1>PREVIOUS ENTRIES</h1>
    </div>

	<!-- Help button & menu stuff -->
    <div style="position: relative;">
      <div class="dropdown">
        <button onclick="myFunction()" class="dropbtn">Menu</button>
        <div id="myDropdown" class="dropdown-content">
          {{#each data.buttons}}
            <a href="{{link}}" ><p>{{name}}</p></a>
          {{/each}}
        </div>
      </div>
    
      <div style=" float:right;">
        <div id="dialog" title="Previous Entries Help" style="display:none">
          <p>You are viewing the entries you previously added.  They are organized by category, and the most recent entry is highlighted in red.  Press "Delete Entry" to remove an entry from the list.</p>
        </div>
        <!--<style> .button2 {background-color: #4CAF50; border: none; color: white; padding: 15px 15px; text-align: center; text-decoration: none; display: inline-block;
          font-size: 16px; margin: 4px 2px; cursor: pointer;}
        </style>-->
        <div class="help-btn">
          <button class="dropbtn" id="dialog_link">Help</button>
        </div>
      </div>
	</div>
	<!-- End help button & menu stuff -->	

<div class="entries page-item">
    {{#each data.entries-sorted}}
      <div class="entry" id="{{id}}" style="{{mystyle}}">
        <p>{{date}}</p>
	      <h4><strong>{{category}} : {{title}}</strong></h4>
	      <p><i>{{description}}</i></p>
	      <!-- <a href="/editentry" onclick="return confirm('Are you sure you want to delete: {{title}} ')"> Delete Entry </a> -->
	      <form action="/removeentry">
		      <input  type='hidden' name='entrytitle' value='{{title}}' />
		      <input style="transform: scale(0.8,0.8) translateX(-12%);" class="dropbtn" onclick="return confirm('Are you sure you want to delete: {{title}}')" type='submit' value="Delete Entry"/>
	      </form>
	      <!-- <a href="/removeentry" onclick="return confirm('Are you sure you want to delete: {{title}} ')"> Remove Entry </a> -->
	       <hr>
      </div>
    {{/each}}
</div>

    






<script>
/* When the user clicks on the button, 
toggle between hiding and showing the dropdown content */
function myFunction() {
	 console.log("myfunction running");
    document.getElementById("myDropdown").classList.toggle("show");
}

// Close the dropdown if the user clicks outside of it
window.onclick = function(event) {
  if (!event.target.matches('.dropbtn')) {
    var dropdowns = document.getElementsByClassName("dropdown-content");
    var i;
    for (i = 0; i < dropdowns.length; i++) {
      var openDropdown = dropdowns[i];
      if (openDropdown.classList.contains('show')) {
        openDropdown.classList.remove('show');
      }
    }
  }
}
</script>
	

  <!--Ugly link back to home page-->
  <!--<p><a href="/">Back &raquo;</a></p>-->




    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet">
    <link href="/css/introHCI.css" rel="stylesheet">

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <!--<script src="https://code.jquery.com/jquery.js"></script> REPLACED WITH OTHER VERSION IN HEADER-->
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
